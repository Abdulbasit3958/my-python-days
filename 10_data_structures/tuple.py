# TUPLE = Ordered, immutable (cannot be changed), faster

coordinates = (10, 20, 30)
print(coordinates)
print(coordinates[0])  # access like a list

# coordinates[0] = 99  # ❌ Not allowed (immutable)
