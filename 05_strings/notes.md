# 📘 String Notes

## 1. Defining a String
```python
name = "Abdul Basit"

2. String Methods

upper() → Converts string to UPPERCASE

lower() → Converts string to lowercase

title() → Converts string to Title Case

3. Finding Substrings

find("A") → Returns the index of "A"

find("Basit") → Returns the index of "Basit"

4. Replacing Text

replace("old", "new") → Replaces part of the string

The original string is not changed